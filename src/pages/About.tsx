import { Container } from "react-bootstrap";

export function About() {
  return (
    <div>
      <h1>About</h1>
      <Container>
        <p>
          This is a simple store template in React + Context API + TypeScript +
          Bootstrap. Only for a portfolio.
        </p>
      </Container>
    </div>
  );
}
